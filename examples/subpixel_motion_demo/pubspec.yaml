name: subpixel_motion_demo
description: Benchmark & visual analyzer for WebParagraph subpixel snapping during slow scroll and zoom.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  web: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
